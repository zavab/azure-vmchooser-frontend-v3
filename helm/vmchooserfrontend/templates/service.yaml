apiVersion: v1
kind: Service
metadata:
  name: {{ include "vmchooserfrontend.fullname" . }}
  labels:
    {{- include "vmchooserfrontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vmchooserfrontend.selectorLabels" . | nindent 4 }}
