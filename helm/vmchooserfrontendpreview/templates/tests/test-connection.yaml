apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vmchooserfrontend.fullname" . }}-test-connection"
  labels:
{{ include "vmchooserfrontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vmchooserfrontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
